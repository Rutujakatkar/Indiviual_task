#include<stdio.h>
//Write a program to Multiply without using Multiplication operator (*).
int main()
{
    int a=2,b=2,mul=1;
    for(int i=0;i<=a;i++)
    {
        mul=a+b;
    }
    printf("\n %d",mul);
    return 0;
}